using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArmController : MonoBehaviour
{
    // add two local way points between which the arm will move, basically picking up material and placing it when needed (when a rover is near)
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
